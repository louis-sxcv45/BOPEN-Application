import 'package:flutter/material.dart';

class VotingFieldWidget extends StatelessWidget {
  const VotingFieldWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}