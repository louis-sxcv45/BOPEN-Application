buildscript {
    repositories {
        google() // Untuk Google Maven Repository
        mavenCentral() // Untuk Maven Central Repository
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Pastikan versi AGP sesuai
        classpath 'com.google.gms:google-services:4.4.0' // Plugin Google Services
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
